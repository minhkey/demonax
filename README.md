# demonax

## Changes

- Changed `tibia`/`tibian` to `demonax` and `Tibia`/`Tibian` to `Demonax` in all NPC files.
- Translated all `.evt` files from English to German.
- Added skinning for specific creatures via changes to `moveuse.dat`:
    - mutated human
    - deer
    - behemoth